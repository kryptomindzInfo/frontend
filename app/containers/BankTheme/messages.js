/*
 * BankTheme Messages
 *
 * This contains all the text for the BankTheme container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BankTheme';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BankTheme container!',
  },
});
