/*
 * BankCreateFee Messages
 *
 * This contains all the text for the BankCreateFee container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BankCreateFee';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BankCreateFee container!',
  },
});
